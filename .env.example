# Exemplo de variáveis de ambiente
# Copie este arquivo para `.env.local` ou `.env` e preencha os valores.
# ATENÇÃO: não comite arquivos `.env` com segredos.

# Variáveis públicas expostas no bundle cliente devem começar com VITE_
# Por exemplo, se o front precisa de uma chave pública:
VITE_GEMINI_API_KEY=
VITE_API_URL=

# Variáveis opcionais
VITE_SENTRY_DSN=

# Observação: chaves privadas NÃO devem estar no front-end. Use um backend/proxy para segredos.
